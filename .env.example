PAWPAL_API_KEY=replace_with_64_hex_characters
APP_ORIGIN=http://localhost:3030
PORT=3030
ATTACKER_LAB_PORT=4040
# Generate with 'openssl rand -hex 32' to acquire 32 bytes of hex characters, (that will be 64 in length)
DOWNLOAD_SIGNING_KEY=replace_with_64_hex_characters
# Set to 1 only when every request passes through exactly one trusted proxy
TRUST_PROXY_HOPS=0
# Generate with 'openssl rand -hex 32' to acquire 32 bytes of hex characters, (that will be 64 in length)
DATA_ENCRYPTION_ACTIVE_VERSION=v1
DATA_ENCRYPTION_KEY_V1=replace_with_64_hex_characters
